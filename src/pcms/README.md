# Discretization Components
The discretization library provides interfaces on top of discretization libraries. This library provides a baseline set of APIs over the discretizations as well as a set of basic coordinate systems.

Current Support:
- Omega_h
- UniformGrid

Planned Future Work:
- CartesianGrid (variable spacing)
- MFEM
- AMREX
