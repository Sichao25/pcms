These adapters are legacy adapters maintained in the coupler because they mix communication and field evaluation operations.

Notes:
1. meshfields adapter is maintained for testing old coupler code while it still exists.
2. XGC field adapter needs to be ported to the new layout / field model